RSpec.describe Estados::Municipios::Brasileiros do
  it "has a version number" do
    expect(Estados::Municipios::Brasileiros::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
