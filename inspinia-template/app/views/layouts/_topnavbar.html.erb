<div class="row border-bottom">
    <%#= if user_signed_in? ? 'logado' : 'nao funcionou'  %>
    <nav class="navbar navbar-static-top d-flex justify-content-end" role="navigation" style="margin-bottom: 0">
      <ul class="nav navbar-top-links navbar-right">
          <li>
              <a href="<%= url_for :controller => 'landing', :action => 'index' %>">
                  <i class="fa fa-sign-out"></i> Log out
              </a>
          </li>
      </ul>
    </nav>
</div>
